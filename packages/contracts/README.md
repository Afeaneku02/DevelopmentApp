# @better-you/contracts

Shared types for the Goals domain: `Goal`, `GoalCategory`, `CreateGoalInput`, and cross-domain constraints (e.g. `MAX_ACTIVE_GOALS`). Per MVP Blueprint §17, this package holds types shared across services and, eventually, clients.
