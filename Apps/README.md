# apps

Client applications (mobile app, web app, API server) per MVP Blueprint §17.

- `web/` — minimal dev-preview UI for Goal Creation Core (see `apps/web/README.md`). Calls `GoalService` directly in-browser; there is no API server yet.
- Mobile app and API server are not yet created — no milestone has required them.
